name: validate-links

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: docker run --rm --volume="$PWD/site:/blog" $(docker build -q .) --link-validation warn /blog
